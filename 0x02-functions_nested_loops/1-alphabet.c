#include "main.h"
/**
 * main - Entry point
 *
 * Description:that prints _putchar, followed by a new line
 *
*/
void print_alphabet(void);
{
	char ch
		for (ch = 'a'; ch <= 'z'; ch++)
			_putchar(ch);

		_putchar('\n');
}
